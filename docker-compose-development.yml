name: aqualegal-web

services:
  frontend:
    container_name: Aqualegal.Web.Frontend
    image: aqualegal-web-frontend-dev:latest
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
      args:
        VITE_PORT: 8080
    ports:
      - 8080:8080
    restart: on-failure
